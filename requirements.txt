# Schwab API Python Starter (Lite) — dependencies.

requests>=2.31.0                  # REST calls
python-dotenv>=1.0.0              # load .env (SchwabAuth.from_env)
cryptography>=42.0.0              # self-signed cert for the localhost OAuth callback
backports.zoneinfo>=0.2.1; python_version<"3.9"  # stdlib zoneinfo for older Python
